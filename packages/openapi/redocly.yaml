extends:
  - recommended
rules:
  no-unused-components: warn
  operation-4xx-response: off
  info-license: off
  security-defined: off
  no-server-example.com: off
  # GET /assets/{assetId}/paid is intentionally 402-only (x402 payment requirements)
  operation-2xx-response: off
