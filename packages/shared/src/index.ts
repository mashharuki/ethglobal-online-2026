/**
 * @truenft/shared - cross-layer domain code.
 *
 * Modules are added in tasks.md Phase 2 (T022-T027):
 *   errors.ts / hashing.ts / kv-format.ts / eip712.ts / manifest.ts / addresses.ts
 * Nothing here may depend on Node-only APIs (must run in Workers / browser).
 */
export const SHARED_PACKAGE_VERSION = "0.1.0";
