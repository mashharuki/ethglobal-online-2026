{
  "name": "@truenft/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-layer domain code for TrueCollective: ErrorCode, EIP-712 types, hashing, manifest schema, KV format. Runs in Workers / Node / browser (no Node-only APIs).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "viem": "^2.56.3",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.0"
  }
}
