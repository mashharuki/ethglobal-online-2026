{
  "name": "agent",
  "version": "0.1.0",
  "private": true,
  "description": "CI verification harness (not a production agent): drives the gateway MCP server discover_assets -> buy_access -> decrypt_content -> analyze with zero human intervention (SC-007 / SC-009).",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.13",
    "typescript": "~6.0.3",
    "vitest": "^4.1.0"
  }
}
