{
  "name": "@subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "TrueCollective Rights Graph - self-hosted The Graph subgraph for RightsNFT / RightsRegistry on Hedera Testnet (discovery and audit only).",
  "scripts": {
    "sync-abis": "node scripts/sync-abis.mjs",
    "manifest": "mustache config/testnet.json subgraph.template.yaml > subgraph.yaml",
    "codegen": "pnpm run manifest && graph codegen",
    "build": "pnpm run codegen && graph build",
    "compile": "pnpm run build",
    "test": "pnpm run codegen && graph test",
    "create": "graph create --node ${GRAPH_NODE_ADMIN:-http://localhost:8020/} truecollective/rights-graph",
    "deploy": "pnpm run build && graph deploy --node ${GRAPH_NODE_ADMIN:-http://localhost:8020/} --ipfs ${GRAPH_NODE_IPFS:-http://localhost:5001} --version-label ${SUBGRAPH_VERSION:-v0.1.0} truecollective/rights-graph",
    "graph-node": "docker compose --file ./graph-node/docker-compose.yaml up -d",
    "graph-node:down": "docker compose --file ./graph-node/docker-compose.yaml down"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2"
  },
  "devDependencies": {
    "assemblyscript": "0.19.23",
    "matchstick-as": "0.6.0",
    "mustache": "^4.2.0"
  }
}
